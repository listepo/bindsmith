// GENERATED BY bindsmith — do not edit.
//
// What the Android binding needs at run time. Apply it once from
// android/build.gradle:
//
//     apply from: 'bindsmith.gradle'
//
// (build.gradle.kts: apply(from = "bindsmith.gradle")), and let
// Flutter build this Android library, in pubspec.yaml:
//
//     flutter:
//       plugin:
//         platforms:
//           android:
//             ffiPlugin: true

repositories {
    maven { url 'https://maven.google.com' }
    maven { url 'https://repo1.maven.org/maven2' }
}

android {
    defaultConfig {
        // Every app using this package keeps the bound classes.
        consumerProguardFiles 'bindsmith-rules.pro'
    }
}

dependencies {
    implementation 'androidx.biometric:biometric:1.2.0-alpha05'
}
