name: win32_tickcount
description: Win32 GetTickCount64 binding generated by bindsmith (winmd driver).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  bindsmith_runtime:
    path: ../../packages/bindsmith_runtime
  ffi: ^2.2.0

dev_dependencies:
  bindsmith:
    path: ../../packages/bindsmith
  lints: ^6.0.0
