name: chartjs_web
description: Chart.js bindings generated by bindsmith (dts driver).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  bindsmith_runtime:
    path: ../../packages/bindsmith_runtime

dev_dependencies:
  bindsmith:
    path: ../../packages/bindsmith
  lints: ^6.0.0
