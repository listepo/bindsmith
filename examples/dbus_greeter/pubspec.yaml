name: dbus_greeter
description: D-Bus Greeter bindings generated by bindsmith (dbus driver).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  bindsmith_runtime:
    path: ../../packages/bindsmith_runtime
  dbus: ^0.7.15

dev_dependencies:
  bindsmith:
    path: ../../packages/bindsmith
  lints: ^6.0.0
