/// Minimal end-to-end: one config, one C header, one generated binding.
///
/// Run from the repository root:
///
/// ```sh
/// dart run bindsmith generate --project example
/// ```
///
/// See `bindsmith.yaml` for the config.
library;

void main() {}
